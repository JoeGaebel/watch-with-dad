name: watch-with-dad-k8s
root: ~/workspace/watch-with-dad

windows:
  - main:
      layout: even-vertical
      panes:
        - watch -n1 kubectl get services
        - watch -n1 kubectl get pods
        -
